[package]
name = "finneo_crypto"
version = "0.1.0"
edition = "2024"
description = "Cross-platform encryption library for Finneo"
license = "MIT"

[lib]
crate-type = ["cdylib", "staticlib"]
name = "finneo_crypto"

[dependencies]
ring = "0.17"
argon2 = "0.5"
